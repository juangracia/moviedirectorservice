package com.example.moviedirector;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main entry point for the Movie Director Service application.
 */
@SpringBootApplication
public class MovieDirectorServiceApplication {
    public static void main(String[] args) {
        SpringApplication.run(MovieDirectorServiceApplication.class, args);
    }
}
